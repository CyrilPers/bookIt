package com.bookIt.demo.enums.code.customer;

import com.bookIt.demo.enums.code.ErrorCodeEnum;

public enum CustomerError implements ErrorCodeEnum {
    CUSTOMER_ALREADY_EXIST,
    CUSTOMER_NOT_FOUND,
}
