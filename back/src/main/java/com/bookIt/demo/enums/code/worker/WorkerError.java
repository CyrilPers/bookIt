package com.bookIt.demo.enums.code.worker;

import com.bookIt.demo.enums.code.ErrorCodeEnum;

public enum WorkerError implements ErrorCodeEnum {
    WORKER_NOT_FOUND,
    WORKER_ALREADY_EXIST,
    PHONE_NUMBER_ALREADY_USED
}
